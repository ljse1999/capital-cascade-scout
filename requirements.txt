feedparser>=6.0
PyYAML>=6.0
yfinance>=0.2.40
# LLM client. The openai SDK drives minimax, GLM (z.ai) and DeepSeek via base_url.
openai>=1.40               # LLM_PROVIDER=minimax (default) | glm | deepseek
